<?

	$css_txt .= '

		/* SETAS */
			.Plugin1 .plugin_seta_left span,
			.Plugin1 .plugin_seta_right span { border-color: var(--COR2); }
		/* SETAS */

		/* PAGG */
			.Plugin1 .plugin_paginacao a,
			.Plugin1 .plugin_paginacao span { width: 8px; height: 8px; border: 2px solid var(--COR2); background: transparent; }
			.Plugin1 .plugin_paginacao a.active,
			.Plugin1 .plugin_paginacao span.active,
			.Plugin1 .plugin_paginacao .plugin_paginacao_item.active a,
			.Plugin1 .plugin_paginacao .plugin_paginacao_item.active span,
			.Plugin1 .plugin_paginacao a.flex-active
			.Plugin1 .plugin_paginacao span.flex-active { width: 14px; height: 14px; background: var(--COR2); }
		/* PAGG */

	';


?>